# ── Frontend (Vite) ─────────────────────────────────────────────────────────────
# The URL of your Render backend API (including the /api prefix path if needed).
# For local development, use: http://localhost:8000
# For production, use your Render service URL.
VITE_API_URL=https://your-render-backend.onrender.com